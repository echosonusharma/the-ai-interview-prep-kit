# PrepPilot frontend — copy to .env.local or .env for local dev
# NEXT_PUBLIC_* is exposed to the browser — never put secrets here.

# Empty = same-origin via next.config rewrites (required for cookie auth).
NEXT_PUBLIC_API_URL=
